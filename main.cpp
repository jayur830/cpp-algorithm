#include <bits/stdc++.h>
#include "./functions.h"
#include "./meta.cpp"
using namespace std;

int main() {
    // combination(vector<int>({ 2, 5, 3, 4, 6 }), 5, 3);
    const vector<string> arr = split("Hello, World, Goodbye", ", ");
    cout << arr << endl;
    return 0;
}
